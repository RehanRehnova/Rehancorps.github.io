/* Anonymous install counter (CounterAPI — free, no passwords/sites sent).
 * Set to false to disable. */
var PASSFILL_COUNT_ENABLE = true;
var PASSFILL_COUNT_NS = 'rehnova';
var PASSFILL_COUNT_KEY = 'passfill-installs';
